# Server-only. Never expose this to the client bundle
# (no NEXT_PUBLIC_ prefix — it is read exclusively in app/api/encourage).
# Get a key at https://platform.claude.com/
ANTHROPIC_API_KEY=sk-ant-...
